import express from 'express'

const balanceLogRoutes = express.Router();

balanceLogRoutes.get("/:id", );
balanceLogRoutes.get('/:id/:balanceLogId', );
balanceLogRoutes.post('/:id/:balanceLogId', );
balanceLogRoutes.delete('/:id/:balanceLogId', );
balanceLogRoutes.patch('/:id/:balanceLogId', );






export {
    balanceLogRoutes
}
